import { place } from "./utils"

export const DEFAULT_TIME = "00:00:00";
export const DEFAULT_STARTTIME = 0;
export const DEFAULT_PLACES: place[] = [];

export const DEFAULT_SETTING_MIN = 1;
export const DEFAULT_SETTING_MAX = 1000;